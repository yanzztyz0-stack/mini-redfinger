// server.js
const express = require("express");
const app = express();
const PORT = process.env.PORT || 3000;

app.get("/", (req, res) => {
  res.send("Server kamu sudah berjalan dengan sukses 🚀");
});

app.listen(PORT, () => {
  console.log(`Server aktif di port ${PORT}`);
});
